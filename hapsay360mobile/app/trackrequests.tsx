import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function trackrequests() {
  return (
    <View>
      <Text>trackrequests</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
